<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.example.butikmodisteshofi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ButikModisteShofi">
        <activity android:name=".HalamanBarang"></activity>
        <activity android:name=".Notifikasi" />
        <activity android:name=".RiwayatPenjualan" />
        <activity android:name=".Beranda" />
        <activity android:name=".DetailTransaksiPenjualan" />
        <activity android:name=".TransaksiPenjualan" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>